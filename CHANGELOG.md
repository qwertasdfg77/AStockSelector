# 更新日志

## 0.3.6

- 新浪 K 线非空但日期未达目标时立即切换腾讯备用源，主备源都精确校验目标收盘日。
- App 更新下载增加 APK 大小安全上限、Content-Length 校验和流式越界保护。
- 后台服务启动失败与协程取消处理更稳，通知权限不再重复请求。
- 后台进度通知限制为最多每秒刷新一次，阶段切换和最终状态仍立即更新，避免系统丢弃通知。
- Gson 升级到 2.14.0，纳入上游 JSON 解析正确性与兼容性修复。
- 修复发布准备脚本的版本同步和 PRD 自动生成，更新安全报告与维护文档。

## 0.3.5

- “建仓三阳”调整为平衡版：连续 3 至 5 根实体至少 20% 的阳线、非涨停光头阳、前一日 3 倍且前 5 日均量 1.5 倍的确认量，并要求 MA20 不低于 5 日前。
- “拉升三阳”调整为平衡版：最近 3 根实体至少 20% 的阳线量价逐日提高，第三阳收盘创近 5 日新高并直接形成信号，不再等待缩量阴线。
- 移除两套三阳战法原有的 75% 缩量、3.5% 回调和 MA10 支撑硬条件。
- 策略规则版本升级，已有日 K 缓存继续保留，旧增量筛选结果会自动失效并重新计算。

## 0.3.4

- 新增“建仓三阳”和“拉升三阳”，今日信号现支持六个预设战法。
- 建仓三阳量化连续阳线、光头阳、前 5 日均量 3 倍放量和 75% 缩量确认。
- 拉升三阳量化连续 3 日价量提高、近 5 日新高、3.5% 以内缩量回调及 MA10 支撑。
- 两套新战法接入多选“且”、同股合并、选择持久化、“新”标识及增量结果表。
- 策略规则版本升级只重算并清理旧策略结果，不删除或重新下载日 K 缓存。

## 0.3.3

- 新浪与腾讯备用日 K 统一使用未复权价格；旧价格口径缓存会自动重建一次。
- 逐股精确校验目标收盘日，旧 K 线和疑似盘中 K 线都不会写入当日信号；全量失败也会先写入失败队列。
- 最低成交额改为全部战法的硬过滤条件；股票主表保留完整有效股票，降低门槛时自动补齐新增范围。
- 零命中结果支持直接复用，增量筛选结果改为批量事务写入。
- 筛选参数持久保存，“新”标记只处理一次，后台服务不再以默认参数意外重启。
- 修复失败重试上限被绕过、门槛外股票拉低覆盖率，以及筛选游标未关闭时写数据库的问题。
- 缓存缺口和可筛选股票改为按目标交易日索引查询，避免刷新前扫描全部 270 日 K 线。
- 股票列表为空或数量异常时自动切换备用源；目标交易日按多数样本确认，避免单个样本异常。
- 重建缓存会同时清理 SQLite 辅助文件；打开系统安装器失败时会保留新版并显示明确原因。
- 增加缓存与快照策略测试、Android Lint、Gradle 校验和、Action 提交固定及签名证书指纹校验。

## 0.3.2

- 第一次刷新时，交易日确认不再先读取完整 A 股列表。
- 改为直接使用固定样本股票读取少量日 K，先确认数据源实际最新交易日。
- 当缓存已经是实际最新交易日时，可跳过完整股票列表读取和全市场补读，减少 App 打开后的第一次刷新耗时。

## 0.3.1

- 智能更新的目标交易日不再只按周末和 15:30 估算。
- 当本地缓存落后日历目标时，先读取少量样本股票的最新日 K 日期，用数据源实际最新交易日作为更新目标。
- 节假日或数据源未出当天 K 线时，避免每次刷新都反复下载不存在的数据。
- 实际目标交易日会短时写入缓存 metadata，频繁刷新时可以直接复用。

## 0.3.0

- Release 流程改为只发布正式签名 release APK，不再上传 debug APK。
- GitHub Actions artifact 和日志保留期改为 7 天，减少旧构建产物堆积。
- Release 发布后自动读取远端 latest.json 并下载 APK，校验版本、大小和 SHA256。
- App 内程序更新增加下载进度条、百分比、重试提示和更清楚的失败原因。
- 下载失败或中断后自动清理未完成 APK，避免残留损坏安装包。

## 0.2.9

- Retrofit 依赖升级到 2.12.0，保持网络请求栈在当前 Android 基线下可通过测试。
- 发布 Action 升级到 softprops/action-gh-release v3。
- Dependabot 暂停 AndroidX 和构建工具类自动升级，避免在 compileSdk 35 / AGP 8.7.x / Gradle 8.9 基线上反复打开失败 PR。

## 0.2.8

- 更新检测和 APK 下载增加 cache-bust 参数，避免 GitHub raw 同名文件缓存导致手机读到旧版本。
- App 内更新继续显示当前版本、最新版本、更新说明、下载进度和校验结果。
- 发布流程保留 APK 签名、SHA256、大小、latest.json 和 tag 一致性校验。

## 0.2.7

- App 内更新改为先检测后下载，显示当前版本、最新版本和更新说明。
- APK 下载增加进度百分比、三次重试、失败清理和更明确的错误提示。
- 发布流程增加 APK 签名、SHA256、大小、latest.json 和 tag 一致性校验。
- 升级低风险 GitHub Actions 版本，并整理 Dependabot 分组和 PR 数量。

## 0.2.6

- 本地筛选新增增量结果表，按规则和交易日缓存每只股票的评估结果。
- 每天更新后只重算本次更新成功或尚未评估的股票，切换战法时优先复用结果表。
- 更新进度细化为读取列表、规划补读、补K线、保存缓存和增量筛选五个阶段。
- 后台前台服务通知增加进度条，补K线和筛选时显示当前进度。

## 0.2.5

- 程序更新下载后校验 APK SHA256 和大小，校验通过后再安装。
- 更新服务优先发布正式签名 release APK，并继续保留自动发布链路。
- 补充发布脚本、Dependabot、CodeQL 和单元测试。

## 0.2.4

- 重建缓存操作新增二次确认弹窗。
- 弹窗警告会说明旧 K 线缓存将被删除、需要重新联网下载最近约 270 个交易日数据、可能耗时和消耗流量。
- 确认按钮改为“删除并重建”，降低误触风险。

## 0.2.3

- 优化本地策略筛选的均线计算，减少重复计算 MA5/10/20/60/250。
- 缓存联网更新改为固定 worker 池，降低大量股票更新时的协程调度开销。
- HTTP 重试等待改为协程 delay，避免阻塞后台线程。
- 缓存保留窗口从约 320 个交易日回退为约 270 个交易日，减少缓存体积和读取量。
- 新增策略单元测试，覆盖年线首板、九阳蓄势、K 线不足和 ST 股票过滤。

## 0.2.2

- 智能更新改为按股票缺口增量更新，只补缺目标交易日、失败或新增股票。
- 新增缓存失败队列，失败股票会持久保存并在后续优先补读。
- 限制同一股票当天重复失败重试次数，避免异常数据拖慢整体更新。
- 设置页新增“重建缓存”按钮，可只重建 K 线缓存而不清除 App 设置。
- 缓存状态新增最新日覆盖率、失败队列数量和可重试失败数量。
- 首次和重建缓存按约 270 个交易日保留 K 线，筛选仍只读取必要窗口。

## 0.2.1

- 新增低位启动策略。
- 今日信号支持四个预设战法。
- 多战法选择改为“且”筛选。
- 同一股票多策略命中时合并显示。
- 新入选股票置顶并显示“新”标识。
- 优化本地缓存筛选窗口和候选股票并行计算。
- 增加 GitHub 程序更新检测。

## 0.1.x

- 初始 Android App。
- 支持年线首板、九阳蓄势、博弈K。
- 支持手机端联网读取和本地缓存。
- 支持智能更新并筛选。
